doctype html
head
    include includes/head
    +head
    title 404 Page not found
    style.
        body {
            padding: 0px;
            margin: 0px;
        }

        .vertical {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            padding: 0;
            font-weight: 200;
            gap: 10px;
        }
body
    h1(class="vertical") This user was not found!
    include ./includes/footer
    +footer()